# ============================================================
# Hatsun RDMS — Environment Variables Template (Split Host)
# ============================================================

# Backend Configuration
PORT=5000
NODE_ENV=production
JWT_SECRET=super_secure_random_production_jwt_key_hatsun_2026
DB_PATH=./backend/database.sqlite
AUTO_SEED=true
CORS_ORIGIN=*

# Frontend Configuration
# Set this in Vercel / Netlify environment variables pointing to your backend
VITE_API_BASE_URL=https://your-backend.onrender.com/api

# Mail Service Configuration (Resend / SMTP)
ADMIN_EMAIL=pavannaik1689@gmail.com
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=resend
SMTP_PASS=your_resend_api_key
SMTP_FROM="Hatsun RDMS" <onboarding@resend.dev>
SMTP_REPLY_TO=hatsun-rdms@proton.me
