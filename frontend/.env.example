# Hatsun RDMS Frontend Environment Variables

# Base URL of the deployed Backend REST API (leave empty for local dev proxy)
# In production on Vercel/Netlify, set this to your hosted backend URL (e.g., https://your-backend-api.onrender.com/api)
VITE_API_BASE_URL=https://your-backend-api.onrender.com/api
